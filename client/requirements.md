## Packages
framer-motion | Essential for portfolio animations and page transitions
react-scroll | For smooth scrolling navigation between sections
emailjs-com | (Optional) For fallback contact form handling if API fails, but we'll use the API route primarily

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  mono: ["'JetBrains Mono'", "monospace"], // Tech/code aesthetic
  display: ["'Space Grotesk'", "sans-serif"], // Modern headings
}
